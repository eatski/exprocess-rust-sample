# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - testes2
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Output commit hash
        run: npm run commithashes
      - name: use node build
        id: node_build_chache
        uses: actions/cache@v2
        with:
          path: dist
          key: ${{ runner.os }}-nodebuild-${{ hashFiles('gitcommithash.log') }}
      - name: Use cargo cache
        if: steps.node_build_chache.outputs.cache-hit != 'true'
        uses: actions/cache@v2
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      - name: Build wasm
        if: steps.node_build_chache.outputs.cache-hit != 'true'
        run: |
          curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
          wasm-pack build --release
      - name: Use node cache
        if: steps.node_build_chache.outputs.cache-hit != 'true'
        uses: actions/cache@v2
        with:
          path: |
            ~/.npm
            node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - name: Node build
        if: steps.node_build_chache.outputs.cache-hit != 'true'
        run: npm ci --production && npm run build
      - name: Deploy
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PICK_ROLE }}'
          channelId: live
          projectId: pick-role
